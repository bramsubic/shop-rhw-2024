import styled from 'styled-components';
import { motion } from "framer-motion"

export const Title = styled(motion.h1)`
    transition: all 2s cubic-bezier(0.165, 0.85, 0.44, 1);
    z-index: 1; 
    font-size: 1em;
`;
